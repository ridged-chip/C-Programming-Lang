#include <stdio.h>

// counts lines of input
int main()
{
				int c, nl;

				nl = 0;
				while ((c = getchar()) != EOF) {
								if (c == '\n' || c == '\t' || c == ' ') {
												++n1;
				}
				printf("%d\n", nl);
				return 0;
}
